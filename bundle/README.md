# Packaging OpenGrinding

1.  **Build**: Build het project met maven in IntelliJ of via de command line:
   ```bash
   mvn clean package
```
2. **Locate the JAR**: Na het bouwen, vind je de JAR file in de `target` directory van de module `bundle`.